import { CreateCategoriesType } from "@/validationSchema/createCategories.schema";

const CreateCategoriesService = async (data: CreateCategoriesType) => {
	try {
	} catch (e) {}
};

export default CreateCategoriesService;
